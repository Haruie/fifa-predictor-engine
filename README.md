# Fifa-Prediction-Engine-with-5-model-ensemble
 A player-aware FIFA World Cup match predictor a 5-model ensemble (Logistic Regression, Random Forest, XGBoost, AdaBoost, KNN) trained on year-specific squad profiles, benchmarked against a historical win-ratio baseline. FastAPI + React.
